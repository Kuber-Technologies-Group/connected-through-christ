# dependencies
/node_modules
/.pnp
.pnp.js

# next.js build output — never commit this
/.next/
/out/
/build

# environment files — NEVER commit these (contain secret keys)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# only commit .env.example (no real values)
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
